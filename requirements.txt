# Core: Python 3.10+ standard library only.
# All API integrations (HTTP, JSON, XML, subprocess) use stdlib modules.
# No external packages are required to run the agents.

# Optional — install for enhanced functionality:
#   requests          # More robust HTTP client (replaces urllib)
#   anthropic         # Claude API for LLM-based lead qualification
#   openai            # GPT-based analysis alternative
#   pandas            # Tabular data manipulation and CSV export
#   sqlalchemy        # SQL database persistence (replace JSON files)
#   pydantic          # Stricter data validation
#   schedule          # Periodic sourcing cycles (cron alternative)
#   python-dotenv     # Load .env files automatically
